package integration

import (
	"testing"
)

func TestFullFlow(t *testing.T) {
	// 1. Запускаем сервер (в тестовом режиме с временной БД)
	// 2. Регистрируем пользователя
	// 3. Логинимся
	// 4. Сохраняем запись локально
	// 5. Вызываем FullSync
	// 6. Останавливаем сервер, запускаем заново (имитируем перезагрузку)
	// 7. Загружаем сессию, делаем Pull – проверяем, что запись есть
	t.Skip("требуется подъём PostgreSQL и gRPC сервера в тесте")
}
